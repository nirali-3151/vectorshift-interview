// controlStyles.ts
// Shared chrome for the form controls, so a change to the border or focus ring
// lands on the input, textarea, and select at once.
// --------------------------------------------------

export const controlClass =
  'min-w-0 flex-1 rounded-sm border border-line bg-surface px-1.5 py-1 text-xs text-ink focus:outline-2 focus:-outline-offset-1 focus:outline-accent';
